TESTS = \
	structured-names \
	cterms

MAUDE_FILES = \
	structured-names.maude \
	cterms.maude

RESULT_FILES = \
	structured-names.expected \
	cterms.expected

EXTRA_DIST = $(TESTS) $(MAUDE_FILES) $(RESULT_FILES)
